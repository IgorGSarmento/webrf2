create table usuarios (
	id serial primary key not null,
	usuario varchar(50) not null,
	senha varchar(50) not null
);

update pg_database set encoding = pg_char_to_encoding('UTF8') where datname = 'dbWebRF2';